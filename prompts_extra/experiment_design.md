Design a CPU-feasible experiment that completes under the configured
five-minute per-run limit. Require at least two baselines, three random seeds
when stochastic, explicit failure criteria, and machine-readable raw metrics.
Do not claim a contribution before it has been measured.

